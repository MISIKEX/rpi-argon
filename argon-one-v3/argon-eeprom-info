set -euo pipefail
tmp="$(mktemp)"; trap 'rm -f "$tmp"' EXIT
curl -fsSL "https://raw.githubusercontent.com/MISIKEX/rpi-argon/main/argon-one-v3/argon-eeprom.sh" -o "$tmp"
chmod +x "$tmp"
sudo bash "$tmp"
